/* Copyright 2020 CSCE240
 */

#include <cassert>
// using assert
#include <cstddef>
// using size_t
#include <iostream>
using std::cout;
using std::endl;
#include <sstream>
using std::stringstream;
#include <string>
using std::string;

// create a function which returns the idx of smallest element in an int array
size_t MinValue(const int array[], const size_t size);


// create a function which returns the largest element in an array
size_t MaxValue(const int array[], const size_t size);


// create a function which copies an int array and returns the value
/*
 * Returns:
 *  new array of size size. Must be deleted by outside code.
 */
void Copy(const int from[], size_t size, int to[]);


// create a function which creates an int array


// create a function which sorts an int array
